@AGENTS.md

## Claude Code

Claude Code reads `CLAUDE.md`, not `AGENTS.md`, so this file imports the shared project instructions above. Keep Claude-specific notes here only when they do not belong in the shared agent contract.

Nested `CLAUDE.md` files should follow the same pattern: import the nearest path-specific `AGENTS.md` and avoid duplicating shared instructions.
